# MERGE — Supabase Migration Progress

Tracks execution of [`plan.md`](./plan.md). Check items off as they land; leave a one-line note + date next to anything non-obvious. Nothing below is checked yet — Phase 0 is the next action.

## Phase 0 — Repository freeze & baseline
- [x] Migration branch created — `feat/supabase-migration`, off `main` @ `59993a9`
- [x] Baseline commit tagged — `pre-supabase-migration`
- [x] `pnpm build` / `pnpm lint` / `pnpm test` output recorded in `baseline.md` — build ✅, test ✅ (1 suite, minimal coverage), lint ❌ (pre-existing, see below)
- [x] `baseline-env.md` written

## Phase 1 — Cleanup & contract stabilization
- [x] ADR-001 (canonical `user_role`) confirmed — `packages/types/index.ts`'s `UserRole` now has all 6 values; `RegisterUserSchema` derives its role enum from a single `SELF_SERVICE_ROLES` list instead of a second hardcoded array. Commit `42f9856`.
- [x] `apps/frontend/src/shared-types/index.ts` de-duplicated — now `export * from '@merge/types'` instead of a byte-identical hand copy. Same commit.
- [x] Fixed `apps/backend`/`apps/frontend` lint tooling — neither had `eslint` installed *at all* (not just missing config; worse than baseline.md first suggested). Added flat config for backend, `next/core-web-vitals` for frontend, plus the real issues that surfaced once lint could actually run: 11 unused imports, 1 unescaped apostrophe (`resident/page.tsx`), and an out-of-tsconfig test glob (`test/security/*.e2e-spec.ts`, fixed via `allowDefaultProject`). `pnpm build`/`lint`/`test` all still green after. Commit `a465f55`.
- [ ] ADR-002 (`property_manager` → `/admin`, RLS-scoped) — decision recorded in plan.md, nothing to implement until Phase 6 (`middleware.ts` doesn't exist yet)
- [x] Orphaned-code table reviewed (plan.md §1.4), no deletions made — correct per Rule 1/5, revisit at Phase 16

**Found while fixing lint, not yet acted on (queued for later phases):**
- `test/security/bola.e2e-spec.ts` and `rbac.e2e-spec.ts` exist but are non-functional placeholder stubs (literal `<ADMIN_A_TOKEN>` bearer strings) and aren't run by `pnpm test` (only `apps/backend/src/modules/apartments/apartment.service.spec.ts` runs today). They're a BOLA/IDOR + RBAC test *intent* worth keeping in mind for Phase 18's RLS test matrix, not code to fix now.
- `apps/backend`'s `test:e2e` script points at `./test/jest-e2e.json`, which doesn't exist — that script has never actually run. Phase 18 scope, not Phase 1.

## Phase 2 — Supabase project setup
- [x] Supabase project created — `Merge`, ref `rohkfyamfwisfuohzkvm`, region `eu-west-1`, `ACTIVE_HEALTHY`. Created by the user directly (CLI login/project creation needs a browser, which this session can't do).
- [x] CLI linked — `supabase link` succeeded using a personal access token scoped to the project's owning account (the machine's default `supabase login` session belongs to a *different* account — TechMart/Thriftshop/UhaiLink/AjiraClub — so linking needed `SUPABASE_ACCESS_TOKEN` rather than the default session). Token stored in gitignored `packages/database/.env`, not the default CLI config, so it doesn't disturb the user's other projects.
- [ ] `supabase start` works locally — still blocked. Image pulls (`edge-runtime` ~236MB, `storage-api`) repeatedly truncate with `unexpected EOF` against public.ecr.aws/ghcr.io — consistent enough across many retries to be a real network/throttling issue on this connection, not bad luck. Not resolved; revisit when convenient (open Docker Desktop, retry `supabase start` in `packages/database`). Not currently blocking anything — Phase 3 proceeded directly against the hosted project instead (see below), by user decision.
- [x] `supabase/` scaffolded under `packages/database/` — `config.toml` (`project_id = "merge"`), `migrations/`, `functions/`, `tests/`, `seed.sql` placeholder. Commit `6ec5620`.
- [x] `.env.example` updated with Supabase vars — `apps/frontend/.env.example` gains `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` (commit `6ec5620`); real values written to gitignored `apps/frontend/.env.local` and `packages/database/.env` (secret key + access token), confirmed untracked via `git status --ignored`.

## Phase 3 — Database schema & migrations
- [x] All 19 migrations (`20260823120000` through `20260823121800`) written to `packages/database/supabase/migrations/` and applied to the **hosted** project (local dev still blocked, see Phase 2 — user decided to proceed directly against the hosted project since it wasn't yet serving any traffic).
- [x] Fixed a real ordering bug found while writing these: the draft plan's `012_notices.sql` had the `notices` table's RLS policy calling `manages_apartment()`, which isn't defined until `015_rls_helpers.sql` — would have failed applying in order. Moved all of notices' RLS into `016_rls_policies.sql` alongside every other table instead of leaving it inline in `012`.
- [x] Improved `settle_booking_revenue()` (business functions migration) to read the commission percentage from `settings.SYSTEM_COMMISSION_PERCENTAGE` instead of a hardcoded `0.10` — that setting exists specifically for this and nothing was reading it before.
- [x] `notices` table + RLS added
- [x] `v_technician_marketplace` view created
- [x] `mv_monthly_revenue` materialized view created + `pg_cron` refresh scheduled (confirmed: `cron.job` has 1 row)
- [x] Verified against the live database via the Supabase Management API (not just "push succeeded"): 34 tables, 67 RLS policies (hand-counted expected from the migration file — exact match), 20 enums, 1 materialized view, 10 seeded categories, 2 seeded settings, 1 cron job, and **every one of the 34 tables has both `relrowsecurity` and `relforcerowsecurity` true** — no table was left accidentally unprotected.

**Incident during this phase, resolved:** the hosted project was not empty when first pushed to — it already had a full, unrelated pre-existing schema (`users`/`roles`/`permissions`/`role_permissions`/`sessions`/`refresh_tokens`/`email_verifications`/`password_reset_tokens`/`resident_documents`/`occupancy_history`/`mv_monthly_occupancy`, a full-text search index, and more — not something this migration created, and not matching either the audited Prisma schema or this plan's design). Investigated via the Management API (`database/query` endpoint — much faster than the local Docker-dependent `db dump`, which was itself stuck on the same network issue) rather than assumed. Confirmed with the user that it was disposable before dropping it: `drop schema public cascade` + recreate with standard Supabase grants + cleared `supabase_migrations.schema_migrations`, then reapplied all 19 migrations cleanly. Worth knowing if `rohkfyamfwisfuohzkvm` turns out to have other unexpected history — this wasn't the first thing ever pushed to it.

## Phase 4 — Supabase Auth
- [x] `@supabase/supabase-js` + `@supabase/ssr` added to `apps/frontend`; `lib/supabase/client.ts` (cookie-based `createBrowserClient`, not localStorage — sets up cleanly for Phase 6's middleware later)
- [x] `services/auth.service.ts` rewritten: `signUp`/`signInWithPassword` wired into register/login pages, replacing the old fetch-to-NestJS calls. Handles the uppercase (`RESIDENT`) → lowercase (`resident`) conversion between the existing Zod DTO and the Postgres `user_role` enum at this one boundary.
- [x] `handle_new_user` trigger verified directly against the live database (not just "push succeeded"): inserted an `auth.users` row the way GoTrue would, confirmed the resulting `profiles` row had correct email/name/role (enum cast worked) and defaulted `status='pending'`, then cleaned up the test row (cascade-deleted correctly).
- [x] Role-based post-login redirect — new `lib/roles.ts` (`DASHBOARD_BY_ROLE`) fixes the hardcoded `/resident` redirect bug; `property_manager` → `/admin` per ADR-002. Same map will be reused by Phase 6's middleware rather than redefined.
- [x] `auth-provider.tsx` moved off `localStorage` — now `supabase.auth.getSession()` + `onAuthStateChange()`, loads the `profiles` row on session change instead of polling `authService.getMe()`.
- [x] Found and fixed while in this file: `dashboard-shell.tsx`'s logout handler still did `localStorage.removeItem('accessToken')` — now calls `authService.logout()` (`supabase.auth.signOut()`).
- [x] `pnpm build`/`lint` both clean across the frontend after these changes.
- [ ] NestJS `auth` module retired — not yet; holding per Rule 5 until this is used in anger for a bit longer, not because anything is still depending on it.

**Open product decision, not resolved here:** the live project has `mailer_autoconfirm: false` (Supabase's default) — a self-registered user must click an email confirmation link before they can sign in at all, on top of the app's own "pending admin approval" gate that `register-client.tsx`'s success screen already describes. Two different gates, and the current success message only mentions the admin one. Needs a product call: keep both gates (update the copy to mention email confirmation too), or turn off `mailer_autoconfirm` requirement for a lower-friction self-serve signup. Not changed here — flagging for a decision.

## Phase 5 — Row Level Security
- [x] RLS helper functions deployed (Phase 3)
- [x] All 34 tables: RLS enabled + forced + policies applied (Phase 3; reconfirmed here after this phase's fixes: 67 policies, 0 tables missing RLS)
- [x] Automated pgTAP test suite written: `packages/database/supabase/tests/rls.sql` (65 tests — not a literal 816-cell grid; every test targets an actual policy boundary, see the file's own header for why). Runs via `supabase test db` once local dev works; verified for real this session via a temporary aggregation wrapper against the hosted project (Management API only returns the last statement's result, so a plain multi-statement run hides per-test output — worked around by inserting each result into a temp table and reading it back in one final query; the wrapper itself was never committed, only `tests/rls.sql`).
- [x] All 65 tests pass, including the 4 mandatory regression tests (wallet cross-access, review non-participant, role self-escalation, technician self-verify)

**4 real bugs found by actually running the suite (not by inspection) — all fixed via corrective migrations:**

1. **Marketplace visibility leak** (`20260823130000_fix_marketplace_visibility.sql`) — `mr_select_involved`'s "open marketplace" clause was `status = 'open'` with no role check at all, despite its own comment claiming it was for "verified technicians." Any authenticated user — any resident in any unrelated apartment — could read every open maintenance request platform-wide. Fixed to actually require the viewer be a verified technician.
2. **RLS recursion, maintenance_requests ↔ bookings** (`20260823130100_fix_rls_recursion.sql`) — `mr_select_involved` queried `bookings`, whose own policy queried `maintenance_requests` right back. Postgres detected the cycle: `infinite recursion detected in policy for relation "maintenance_requests"`. Fixed with two new `SECURITY DEFINER` helpers (`technician_assigned_to_request`, `resident_owns_request`) that break the cycle — the same pattern `is_admin()`/`manages_apartment()` already used safely.
3. **RLS recursion, chat_participants self-reference** (`20260823130200_fix_chat_participants_recursion.sql`) — `cp_select_own_chats` queried `chat_participants` from within `chat_participants`' own policy (a same-table self-reference — exactly the pattern Supabase's own RLS guidance warns causes this). `chats_select_participant`/`messages_select_participant`/`messages_insert_participant` had the same issue one hop removed. One new helper (`is_chat_participant`) fixed all four.
4. **Ambiguous self-reference, technicians** (`20260823130300_fix_technician_self_update_ambiguity.sql`) — `technicians_update_self`'s `WITH CHECK` subquery referenced `public.technicians` without an alias, so `technicians.id` inside the subquery matched every row instead of just the one being updated: `more than one row returned by a subquery used as an expression`, on *any* self-update, not just verification-status changes. Fixed by aliasing the inner scan. (Confirmed empirically that WITH CHECK self-references don't hit the same recursion guard SELECT-policy self-references do — `profiles_update_own`'s equivalent pattern already worked correctly, which is why only this one needed a fix rather than every self-referencing WITH CHECK.)

**2 test-methodology bugs found and fixed in the test suite itself** (not database bugs): 6 of the original test assertions used `throws_ok` expecting an exception for writes blocked by a policy's `USING` clause — but a `USING` failure silently filters the row (0 rows affected), it doesn't raise. Only `WITH CHECK` failures raise. Rewrote those 6 as attempt-then-verify-unchanged checks instead. Also found Postgres won't allow a data-modifying CTE nested inside a scalar expression (`(with x as (update ... returning 1) select count(*) from x)`), which the first fix attempt used — reworked to a plain two-statement attempt/verify pattern instead.

## Phase 6 — Frontend Supabase plumbing
- [x] `lib/supabase/client.ts` (Phase 4) + `server.ts` (this phase, `@supabase/ssr`'s `createServerClient` for Server Components/Route Handlers)
- [x] `middleware.ts` created and protecting all 4 dashboard prefixes — reuses `lib/roles.ts`'s `DASHBOARD_BY_ROLE` (inverted to path→allowed-roles) rather than redefining the mapping
- [x] `hooks/`, `queries/` structure established (`queries/profiles.ts` + `hooks/use-profile.ts`, the pattern Phases 7-10 repeat: page → hook → query fn → Supabase). `mutations/` deferred until a phase actually needs one — an empty folder now would be pure ceremony
- [x] Error-mapping layer (`lib/errors.ts`) — covers what exists today (RLS denial 42501, `transfer_wallet_funds`'s `insufficient_funds`, the unique constraints already in the schema); extend as later phases add new constraints/RPCs, not preemptively
- [ ] Pagination/soft-delete filters standard in every list query — no list queries exist yet to apply this to; real target is Phases 7-10, noted here so it isn't forgotten once those start

**Verified for real, not just "it builds":**
- `pnpm --filter frontend build` and `lint` both clean; build output confirms `ƒ Middleware 84.9 kB` is actually bundled.
- Ran the dev server and hit it with real HTTP requests: unauthenticated `/admin`, `/landlord`, `/resident`, `/technician` all correctly `307 → /login`; unprotected `/` and `/login` return `200`.
- Created a real, confirmed test account (had to manually populate `auth.identities` and empty-string several `auth.users` token columns — GoTrue's password-grant login 500s with a generic "Database error querying schema" if a user row is created by raw SQL without these; worth remembering for any future manual test-user setup), signed in for a real session, hand-built the exact `sb-<project-ref>-auth-token` cookie `@supabase/ssr` expects (format confirmed by reading its source, not guessed — `base64-` + base64url JSON, chunked only above 3180 chars, this one didn't need chunking), and hit the running dev server with it directly: resident session → `200` on `/resident`, `307 → /login` on `/admin` (wrong role, correctly blocked). Also hit the REST API directly with the real access token to confirm `queries/profiles.ts`'s exact query returns the right row — the same call the `useProfile` hook makes client-side, which curl can't observe post-hydration since it doesn't execute JS. Test account deleted afterward, no residue.
- Wired real name/initials (pure identity data, no per-dashboard joins needed) into all 4 dashboard layouts via `useProfile`, replacing that slice of `lib/mock/*.ts`. Subtitle text that needs domain joins (resident's unit/building, admin's apartment) intentionally left on mock data with a comment — that's Phases 7-10's job, not this one's.

## Phase 7 — Resident dashboard
- [x] Overview wired — real stats (active requests, wallet balance, escrow held, notice count), real active-requests list, real wallet snapshot, real recent transactions, real notices preview. `lib/mock/resident.ts` deleted (nothing imports it anymore).
- [x] Maintenance wired — real list (`maintenance_requests` joined to `categories`/`bookings`/`technicians`/`profiles`), real create via `NewRequestModal` (now uses real `categories`, not the old hardcoded list that didn't match the seeded ones). "Attach Photos" stays disabled with a "coming soon" label — genuinely blocked on Phase 12 (Storage), not faked. Removed the dead "Message {technician}" button (audit flagged it as a no-op with no handler at all — chat is Phase 14, not worth faking a button for).
- [x] Wallet wired — real balance + real escrow-held (computed from pending `maintenance_escrow` transactions, since the schema has no persistent escrow-balance column) + real transaction history. `TopUpModal` no longer fakes a successful deposit — shows an honest "not available yet, contact your admin" state instead, since real top-ups need Phase 13's `payments-initiate` Edge Function and its gateway credentials.
- [x] AI Assistant wired — conversations and messages now genuinely persist to `ai_conversations`/`ai_messages`. The reply logic is still the client-side keyword matcher (real LLM replies via `ai-diagnose-proxy` are Phase 13 — needs a server-held secret this component can't hold).
- [x] Community/notices wired — real `notices` table, scoped to the resident's own apartment by RLS.

**2 real schema gaps found while wiring, fixed via migrations (not deferred):**
- Nothing ever created a resident's `wallets` row — no trigger existed, and `wallets` deliberately has no client INSERT policy (audit's top wallet finding). A fresh resident would have hit a hard error the first time they opened the Wallet page. Fixed with `20260823140000_auto_create_wallets.sql` — auto-creates one wallet per resident/technician/landlord on creation (also covers Phases 8-9 before they hit the same gap).
- The `notices` table (added in Phase 3) only had `priority`; the already-built Community page UI filters/displays by category (Maintenance/Security/Community/Billing), which wasn't modeled at all. Fixed with `20260823140100_add_notice_category.sql` rather than gutting a working UI feature to match an incomplete schema.

**Verified against the live database, not just build success:** created a full fixture resident (auth user, apartment, building, unit, resident row) and confirmed via the real REST API with a real session token: the wallet auto-create trigger fires: `getMyResidentContext`'s 3-table join, `getMyWallet`, `getApartmentNotices` (with the new category column), `createMaintenanceRequest`'s insert, `getMyMaintenanceRequests`'s 4-table nested join, and `ai_conversations`/`ai_messages` inserts all return exactly the shape each query function expects. All fixtures cleaned up afterward, confirmed zero leftover rows. `pnpm build`/`lint` clean throughout, including after deleting the now-unused mock file.

Closed the Phase 6 TODO in `resident/layout.tsx` while here: the shell subtitle now shows the real unit/apartment via `useResidentContext`, not mock data.

## Phase 8 — Technician dashboard
- [x] Jobs wired — real open-marketplace list (RLS already scoped this to verified technicians only, from the Phase 5 fix), real "Accept Job" (creates a booking; see the new INSERT policy below). Dropped `distanceKm`/`estimatedPayout` from the card display — neither is modeled (no geo-distance calc, no pre-booking payout estimate) and showing fabricated numbers would be worse than omitting them.
- [x] Bookings + status transitions wired — real list, real Accept/Decline for admin-assigned (`PROPOSED`) bookings, real progression buttons. Invalid transitions are now rejected at the database level (see below), not just hidden by only showing the "next" button in the UI.
- [x] Earnings wired — real wallet balance, real "pending payout" (completed jobs with no crediting transaction yet — see note below on why this doesn't use `revenue_shares`), real transaction history. `WithdrawModal` shows an honest "not available yet" state instead of faking a payout, same reasoning as the resident wallet's `TopUpModal`.
- [x] Reviews wired — real reviews joined to reviewer name and the job's unit/building.
- [x] Profile wired — real stats, real availability toggle (persists now, previously local-only), real skills (technician's assigned categories) and certifications. Hourly rate now has somewhere to live (see schema gap below).

**3 real schema/RLS gaps found while wiring, fixed via migrations:**
- `technicians` was missing `hourly_rate` and a human-readable `service_area` — both shown in the already-built profile page, neither modeled (only lat/long existed, no readable location text).
- **No RLS policy anywhere let a technician actually accept an open job.** `bookings` only had an admin "for all" policy and a technician self-update policy for a booking that *already exists* — the self-serve "Accept Job" flow had no INSERT path at all. Added `bookings_insert_technician_self_accept`, plus a trigger that moves the request out of the open marketplace (`status = 'assigned'`) the moment a booking is created, and a second trigger that enforces the valid booking-status state machine at the database level (this phase's plan explicitly committed to rejecting invalid transitions, not just gating who can write).
- **A more serious latent bug, found by testing a real cross-user join, not by inspection:** `profiles`' RLS (self/admin only) silently breaks every join that shows *someone else's* name — a resident viewing their assigned technician's name, a technician viewing a reviewer's name. PostgREST doesn't error on an RLS-blocked embedded relationship, it just returns `null`, so this was invisible in Phase 7's own verification (which only ever read a resident's own request before any technician was assigned — the sub-join was empty, never exercised). Fixed with a second, permissive `profiles` policy making it readable to any authenticated user, matching the `apartments`/`units` "directory, not secret" precedent already flagged from Phase 5 — **and flagged the same way**: this makes every column readable (including email/phone_number) to any authenticated user, not just name/avatar; the app only ever *selects* the safe columns in cross-user queries, but that's a code convention, not a database-enforced boundary. A properly scoped fix (public-safe view, or column-level grants) is future work if this turns out to matter. Re-verified the same fix closes the identical Phase 7 bug (resident viewing their technician's name).

**Simplified, not faked:** `getMyTechnicianWallet`'s "pending payout" deliberately doesn't use `revenue_shares` (that table is `admin`-only by RLS design) — it diffs completed bookings against `transactions` crediting the technician's wallet instead, since PostgREST's filters take a literal value list, not an arbitrary SQL subquery, and this is honestly computable from tables the technician can actually read. Also dropped "earned this month" / "earned all-time" stat tiles from Earnings — computing them accurately would need either a dedicated aggregate (Phase 10/11 territory) or full pagination-complete client aggregation; showing a number based on only the first page of transactions would be a wrong number, not a shortcut.

**Verified against the live database, not just build success:** full fixture flow — resident, technician (verified, with a category and hourly rate/service area), apartment/unit, an open request. As the technician: accepted the job via REST (confirmed the new INSERT policy allows it and the auto-assign trigger fires), attempted an invalid `accepted → completed` jump (confirmed rejected with `invalid_status_transition`), then walked the valid chain `accepted → in_route → work_started → completed`. Confirmed `jobsCompleted` count and the full `technician_categories` join return correctly. As the resident: submitted a review, then re-verified as the technician that the reviewer's name and the job's unit/building resolve correctly (post-fix) — and separately re-verified the identical Phase 7 join (resident viewing their technician's name) is also fixed. All fixtures cleaned up after, confirmed zero leftovers. `pnpm build`/`lint` clean, mock file deleted.

## Phase 9 — Landlord dashboard
- [x] Properties wired — real portfolio via `landlord_apartments`, real occupancy computed from unit status. Dropped the mock's `MANAGED_COMPLEX`/`STANDALONE_UNIT` distinction and `managedBy` name — neither is really a separate concept in the schema (a "standalone unit" is just an apartment with one building/unit, already fully expressible) and resolving the managing property_manager's name would need another join not worth it for this pass.
- [x] Vacancies wired — real list, real applicant counts, real publish/archive. `CreateListingModal` now actually inserts (previously faked success **and discarded the input entirely** — the audit's specific finding for this modal) and the form now also collects `bathrooms`/`deposit_amount`, both NOT NULL columns the old form never asked for at all.
- [x] Applications wired — real list scoped automatically by RLS (`va_select_applicant_or_owner` already restricts to the landlord's own vacancies with no explicit filter needed), real Mark Reviewing/Approve/Decline.
- [x] Finance wired — real wallet balance + real transaction history. Dropped the fabricated "8% management fee" breakdown (gross rent / management fee / net payout) — no `management_fee` setting exists anywhere (unlike the technician commission, which is a real stored `SYSTEM_COMMISSION_PERCENTAGE` setting used by a real function), and nothing yet moves rent payments into a landlord's wallet at all (that pipeline doesn't exist yet — Phase 13 territory). Showing a fabricated breakdown would be worse than a plain balance.

**1 real schema gap found while wiring:** the already-built `CreateListingModal` collects a neighborhood for every listing, but `vacancies` only had location reachable through an optional `unit` join — fine for a listing tied to an existing modeled unit, but most brand-new landlord listings won't have one yet (that's exactly the "standalone unit" case). Added a direct `neighborhood` column (`20260823160000_add_vacancy_neighborhood.sql`) rather than making a vacancy's location depend on internal unit bookkeeping it may not have.

Also added `getPublishedVacancies()` to `queries/vacancies.ts` now (same query shape as the landlord's own list, filtered to `published`) — Phase 15's marketing-site rewiring becomes a drop-in swap instead of new work.

**Verified against the live database, not just build success:** full fixture — landlord with a managed apartment (2 units, 1 occupied), an applicant resident. Confirmed the landlord wallet auto-creates (same trigger as Phase 7/8), `getMyProperties`' nested occupancy join returns the correct 1/2 split, created and published a vacancy for real, had the applicant submit a real application, confirmed the landlord's cross-user view of the applicant's name resolves correctly (exercising the Phase 8 `profiles` directory-read fix again, this time for a third relationship), approved the application, and confirmed the vacancy's `applicantCount` reflects it. All fixtures cleaned up after, zero leftovers. `pnpm build`/`lint` clean, mock file deleted.

## Phase 10 — Admin dashboard
- [x] Overview wired — real occupancy/active-residents/open-requests/pending-verifications tiles, real "Needs Attention" list, real verification-queue preview, real revenue snapshot, real activity feed (honestly empty — see gap below).
- [x] Properties wired — real buildings + units, admin-wide (not property_manager-scoped; see design note below).
- [x] Residents wired — real list with search, real lease dates, real status (`PENDING`/`ACTIVE`/`SUSPENDED`/`DEACTIVATED` — the mock's fictional `ARREARS` status doesn't exist in the schema and wasn't invented).
- [x] Technicians (verify/reject/suspend/reinstate) wired — real roster + verification queue. Verification-status enum has 4 real values (`pending_verification`/`verified`/`suspended`/`rejected`), one more than the mock's 3 — pending applicants now get a real Approve/**Reject** choice instead of Approve/Suspend.
- [x] Maintenance (assignment) wired — real request list, real `AssignTechnicianModal` that inserts a real `proposed` booking (previously `setTimeout` + discarded input, same faked-success pattern as Phase 9's listing modal before it was fixed).
- [x] Finance wired — real platform-revenue chart (via a new admin-only RPC, see below), real escrow/pending-payout tiles, real transaction list with resolved counterparty names.
- [ ] All 4 audit §17 cross-dashboard scenarios pass — deferred to Phase 15's dedicated cross-dashboard verification pass, per plan.md.

**2 real schema/RLS gaps found before writing a single query, not after** (reasoned through by re-reading the RLS design against what this phase needed, the same way Phase 8's booking-INSERT gap was found):
- **`manages_apartment()` never checked `apartment_admin` at all** — only `is_super_admin()` OR a `property_manager_apartments`/`landlord_apartments` join. Every apartment-scoped write policy (`apartments`, `buildings`, `units`, `notices`) and the `residents_select_self_or_manager` read policy depend on this function, so an apartment_admin — the role this entire dashboard is built for — could not even read the residents list. `is_admin()` already treats `apartment_admin` as platform-wide everywhere else in this schema; `20260823170000_admin_dashboard_gaps.sql` makes `manages_apartment()` consistent with that. Confirmed this was a real, load-bearing bug (not theoretical) by hitting `getAllResidents`/`getAllUnits` as `apartment_admin` before the fix — both returned empty; after the fix, both returned the fixture rows.
- **`mv_monthly_revenue` had zero access control.** Postgres doesn't support `ALTER MATERIALIZED VIEW ... ENABLE ROW LEVEL SECURITY` — the matview (Phase 3) was relying entirely on Supabase not auto-exposing new relations, which isn't a real access boundary. Fixed with the standard workaround: `revoke all ... from anon, authenticated` on the matview directly, plus a new `get_monthly_revenue()` SECURITY DEFINER function that checks `is_admin()` itself and is now the only sanctioned read path. Verified both directions live: an `apartment_admin` call succeeds (empty result — no `commission_fee` transactions exist yet, which is honest, not a bug), a `resident` call is rejected with `insufficient_privilege` (added a message mapping for it in `errors.ts`).

**1 more silent-failure class found and closed, not just worked around:** `technicians_admin_verify` restricts verification writes to `is_super_admin()` specifically (deliberate — matches the original backend, `apartment_admin` was never meant to verify technicians). Under RLS, an `apartment_admin`'s `UPDATE` against a blocked row doesn't error — it silently matches zero rows, which is exactly the "fake success" the ground rules rule out. Confirmed this live (a real PATCH as `apartment_admin` returned `200 []`, no error) before deciding on a fix rather than after. Fixed at the query layer by chaining `.select().single()` onto the update in `setTechnicianVerification` — `.single()` throws `PGRST116` when zero rows come back, which now has a real mapped message ("You don't have permission to do that, or it no longer exists.") instead of either a silent no-op or a raw Postgres error reaching the UI.

**Design decision, not a bug:** admin-wide queries (`getAllUnits`, `getAllResidents`, `getAllTechnicians`, `getAllMaintenanceRequests`, `getAllTransactions`) are intentionally platform-wide with no apartment/property_manager scoping, matching `is_admin()` treating both admin roles as full-platform throughout this schema's RLS — not scoped the way `queries/landlords.ts` scopes a landlord to their own `landlord_apartments`. If per-apartment admin scoping is ever wanted, it's an RLS/query change together, not a frontend-only filter.

**Simplified, not faked:** "Monthly Revenue" on the finance page is platform commission (`commission_fee` transactions, what `mv_monthly_revenue` actually aggregates), labeled as such — not gross rent collected, which nothing in this schema tracks yet. Dropped the mock's "Rent in Arrears" tile entirely — there's no due-date/arrears concept modeled anywhere (leases have `lease_start`/`lease_end`, not payment schedules), so it would have been a fabricated number. "Recent Activity" queries the real (currently unpopulated) `audit_logs` table and renders an honest empty state rather than synthesizing entries — nothing writes to it until Phase 11's business-logic triggers land.

**Verified against the live database, not just build success:** full fixture — apartment/building/unit, a resident, a `pending_verification` technician, an `apartment_admin` user, a `super_admin` user, an open maintenance request — all signed in for real sessions via the Auth password grant (not service-role). Confirmed live: `getAllUnits`/`getAllBuildings`/`getAllResidents`/`getAllTechnicians`/`getAllMaintenanceRequests` all return correctly as `apartment_admin` (proving the `manages_apartment()` fix); `setTechnicianVerification` as `apartment_admin` returns `200 []` (blocked, no error) while the identical call as `super_admin` succeeds and updates `verification_status`; `assignTechnician` as `apartment_admin` inserts a `proposed` booking and the existing `mark_request_assigned` trigger (Phase 8) correctly flips the request to `assigned`; `get_monthly_revenue()` succeeds (empty) for `apartment_admin` and is rejected with `insufficient_privilege` for a `resident`; the escrow/pending-payout/recent-activity queries all execute cleanly against zero matching rows. All fixtures cleaned up after via cascade delete from `auth.users`, confirmed zero leftovers across all 9 affected tables. `pnpm build`/`lint` clean, `lib/mock/admin.ts` deleted.

## Phase 11 — Postgres RPCs & business logic
- [x] `transfer_wallet_funds` deployed (since Phase 3) — sequential correctness re-verified live this phase; concurrency testing is explicitly Phase 18's job (financial acceptance tests), not repeated here.
- [x] `settle_booking_revenue` deployed + idempotency verified — both the bookkeeping-row idempotency (already existed, `on conflict`) and the wallet-transfer idempotency (new this phase, see gap below) confirmed live: a direct second call after settlement makes no additional transfer and no additional `commission_fee` transaction.
- [x] `recalc_technician_rating` trigger — already deployed since Phase 3, unchanged this phase.
- [x] Notification triggers (booking assignment + every status change, technician verification, vacancy application submitted + decided, new chat message) — all 6 verified live, see below.
- [x] Audit-log triggers verified — scoped to the 3 admin-driven decisions the Phase 10 activity feed actually surfaces (technician verification, technician assignment, application decision); no Edge Function writer needed for this phase's scope.

**`accept_booking`/`verify_technician` from the plan's RPC table were deliberately not built as RPCs** — both are already plain RLS-gated `update`s from Phases 8/10 (`updateBookingStatus`, `setTechnicianVerification`), matching the plan's own stated preference ("prefer the trigger... unless it needs additional side effects"). The trigger is attached to the existing write instead of adding a second call.

**1 real financial gap found before wiring any of this, by reading the code, not by a failed test:** `settle_booking_revenue` (Phase 3) looks up a `platform_commission` wallet — a valid `wallet_type` enum value — but nothing anywhere (no seed, no trigger) ever created that wallet, and nothing ever called this function either. The first real completion would have transferred the platform's cut into a `null` recipient silently. Fixed in `20260823180000_business_logic_triggers.sql`: seeded the singleton platform wallet, and hardened the function to `raise exception` on a missing platform or technician wallet instead of proceeding, plus made it idempotent against the *transfer* (not just the bookkeeping row) by checking `revenue_shares.is_settled` before doing anything.

**Confirmed live, not assumed:** with the technician wallet unfunded (the real current state — nothing pays a technician into their wallet yet; that's Phase 13's payments-webhook job), completing a real booking correctly raises `insufficient_funds` and rolls back rather than fabricating a payout. This is expected today, not a bug: `total_amount` defaults to `0` on every booking the frontend actually creates (no invoicing exists yet either — flagged back in Phase 8), so `0%` of `0` is `0` and every real completion today settles a harmless `KES 0` revenue share. Simulated a funded wallet to test the real math too: `KES 1,000` total → `KES 100` platform fee / `KES 900` technician share, both wallets balanced correctly, exactly one `commission_fee` transaction.

**1 more real gap found while building the application-notification trigger, not fixed (correctly out of this phase's scope):** there is no "submit application" mutation anywhere in the frontend yet — only `updateApplicationStatus` (the landlord's approve/decline) exists. `vacancy_applications` INSERT is scoped to the applicant themselves (`va_insert_self`, `applicant_id = auth.uid()`), so the trigger was verified with a manual insert carrying that field. Submitting an application is squarely Phase 15's "public marketplace" work, not Phase 11's — noted here so Phase 15 doesn't have to rediscover it.

**Verified against the live database, not just build success:** full fixture (resident, verified technician, super_admin, landlord + published vacancy, a direct chat) with real Auth sessions for each. Walked the entire chain live: admin verifies technician → notification + audit fire; admin assigns technician (`proposed` booking) → technician notification + audit fire; technician walks `accepted → in_route → work_started → completed` → resident gets all 4 status notifications in order, and completion correctly blocks on an unfunded wallet, then correctly settles once funded (exact 90/10 split, single transaction, confirmed idempotent against a direct re-call); resident applies to the vacancy → landlord notified; landlord approves → applicant notified + audit logged; resident sends a chat message → only the *other* participant is notified, never the sender. All fixtures cleaned up after (including resetting the platform wallet's test-inflated balance back to `0`), confirmed zero leftovers across every affected table. `pnpm build`/`lint` unaffected (this phase is pure database — no frontend files changed).

## Phase 12 — Supabase Storage
- [x] 6 buckets created with correct public/private policies — `avatars`/`property-media`/`vacancy-media` public read + scoped write; `maintenance-media`/`documents`/`chat-media` private, scoped both ways. Every private-bucket policy reuses an existing RLS helper (`resident_owns_request`/`technician_assigned_to_request` from Phase 5, `manages_apartment` from Phase 10, `is_chat_participant` from Phase 5) keyed off `(storage.foldername(name))[1]` as the owning entity's id, rather than re-deriving the same ownership joins a second time. One new helper added for `documents`: `owns_or_manages_application()` (applicant or the vacancy's landlord).
- [x] `file_records` row created on every upload — via a shared `lib/storage.ts#uploadFile()` helper (upload to bucket, then insert the `file_records` row), not duplicated per call site.
- [x] Cross-user access negative test passes — confirmed live (see below).

**Only one bucket got real UI wiring this phase: `maintenance-media`.** It's the one place the frontend already had an inert stub — Phase 7's `NewRequestModal` shipped a disabled "Attach Photos (coming soon)" button specifically noting it was deferred to this phase. Wired it for real: a working multi-file picker (max 5 photos, client-side size-capped to the bucket's 20MB limit), uploaded after the request row exists (the bucket's RLS needs a real `maintenance_requests.id` to check ownership against, so upload can't happen before the insert), then `media_keys` is updated with the resulting keys. The other 5 buckets (`avatars`, `documents`, `property-media`, `vacancy-media`, `chat-media`) are real infrastructure now — created, policied, and spot-checked — but have no frontend consumer yet, because none of those upload flows exist in the UI at all yet (not stubbed-and-disabled the way maintenance photos were; genuinely absent). Building that UI now would be scope creep beyond what this phase's own objective asks for ("replace the currently-conceptual-only Storage *references*" — there isn't one for these 5 yet). They're ready for whichever future phase adds that UI.

**1 real gap found while wiring the application-notification trigger in Phase 11, surfaces again here:** `documents` (vacancy-application paperwork) has no producer either, for the same reason — there's no "submit application" UI yet. Noted once in Phase 11, not re-litigated here.

**Verified against the live database, not just build success:** full fixture — two unrelated residents, one maintenance request owned by the first. As the owner: uploaded to `maintenance-media/{request_id}/...` (succeeded) and downloaded it back (succeeded). As the unrelated resident: attempted to upload into the *same* folder (blocked, `403`/RLS) and attempted to download the first resident's file by its exact known path (blocked — `404 Object not found`, not a `403`, so the policy doesn't even leak that the object exists) — this is Phase 12's own stated exit criterion, confirmed for real rather than assumed from reading the policy. Spot-checked `avatars` too: owner upload succeeds, a different user's overwrite attempt on the same path is blocked, and an unauthenticated request can still read it (public bucket). All test objects deleted via the Storage API (direct SQL `DELETE` on `storage.objects` is blocked by Supabase itself — used the service-role key for this one-off cleanup, the documented exception for when it's allowed), all fixture rows cleaned up, confirmed zero leftovers. `pnpm build`/`lint` clean.

## Phase 13 — Edge Functions
- [ ] `payments-initiate`
- [ ] `payments-webhook` (idempotency verified)
- [ ] `ai-diagnose-proxy`
- [ ] `reports-export`
- [ ] `assign-technician` (trigger-based, not polling)

## Phase 14 — Realtime
- [ ] Notifications subscription
- [ ] Chat subscription
- [ ] Per-open-booking status subscription
- [ ] Confirmed no other channels exist

## Phase 15 — Public marketplace & cross-dashboard verification
- [ ] Marketing vacancies page reads real `vacancies` table
- [ ] All 4 audit §17 scenarios re-verified end-to-end

## Phase 16 — Mock data removal
- [ ] `lib/mock/admin.ts` removed
- [ ] `lib/mock/landlord.ts` removed
- [ ] `lib/mock/resident.ts` removed
- [ ] `lib/mock/technician.ts` removed
- [ ] Orphaned files deleted (`app/dashboard/page.tsx`, empty `(auth)/login/`, `unit-grid.tsx`, marketing `mockVacancies`)

## Phase 17 — NestJS retirement
- [ ] Module-by-module retirement table (plan.md Phase 17) fully checked off
- [ ] `apps/backend` removed or archived
- [ ] `apps/ai-service` confirmed kept, caller updated to Edge Function

## Phase 18 — Testing
- [ ] Unit tests
- [ ] Database/RLS/RPC tests
- [ ] Integration tests
- [ ] End-to-end lifecycle test
- [ ] Security acceptance tests (7 scenarios)
- [ ] Financial acceptance tests

## Phase 19 — Observability & performance
- [ ] Sentry wired (frontend + Edge Functions)
- [ ] Query-plan review on high-traffic views
- [ ] Pagination confirmed shipped, not just planned

## Phase 20 — Staging & seed data
- [ ] `seed.sql` covers all roles + all status values
- [ ] Staging confirmed on sandbox-only payment credentials

## Phase 21 — Production deployment
- [ ] Full readiness gate (plan.md Phase 21) checked off
- [ ] Production deploy complete
- [ ] Post-deploy smoke tests pass
