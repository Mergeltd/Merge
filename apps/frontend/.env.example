NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_SITE_URL=

# Supabase (docs/migration/plan.md Phase 2). Both are safe for the client —
# the anon key is public by design, authorization comes from RLS
# (docs/migration/plan.md Phase 5), never from keeping this key secret.
# Never put the service-role key in this file or anywhere under apps/frontend.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
